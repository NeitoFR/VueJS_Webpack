const state = {
    scenarios: [{
        name : "A",
        iot : "light"
    }]
}

const mutations = {
    addScenario(state, data) {
        
    }
};

export default {
    namespaced: true,
    state, 
    mutations
}